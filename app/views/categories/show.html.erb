<h1><%= @category.title %></h1>


<% @items.each do |item| %>
  <p><%= item.title %> | <%= number_to_currency(item.price) %> |
    <% if current_user %>
   <%= button_to 'Add to Cart', line_items_path(item_id: item) %></p>
   <% end %>
<% end %>